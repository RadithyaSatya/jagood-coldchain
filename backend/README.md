# backend

Reserved for a future platform-level API gateway / BFF that aggregates the
individual services under `services/` for the frontend.

Not implemented yet. The Smart Route Planner's own FastAPI backend currently
lives at [`services/ai/route-planner/`](../services/ai/route-planner/) and is
called directly by the frontend during this phase.
