# AI Explain

Not implemented yet.

Will explain recommendations, risks, and simulation results (e.g. the Smart
Route Planner's `trigger_reason` and XGBoost feature importances) in clear
language via an LLM layer. See the root
[README](../../../README.md#key-features) for the feature description and
`services/ai/route-planner`'s PRD (FR-8) for the raw inputs this service is
expected to consume.
